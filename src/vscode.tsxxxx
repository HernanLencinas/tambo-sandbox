import * as vscode from 'vscode';

export class VSCextention {

    async getAutoPushStatus(): Promise<boolean> {

        const config = vscode.workspace.getConfiguration("tambo.sandbox");
        return config.get("push", true);
        
    }

}
